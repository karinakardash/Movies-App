export enum LinkButtons {
  HOME = "Home",
  TRENDS = "Trends",
  FAVORITES = "Favorites",
  SETTINGS = "Settings",
}

export enum AppPages {
  HOME = "/home",
  TRENDS = "/trends",
  FILM_PAGE = "/home/movie",
  FAVORITES = "/favorites",
}

export enum FetchStatus {
  FAILURE = "failure",
  PENDING = "pending",
  SUCCESS = "success",
}
